services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
    volumes:
      - app-data:/app/.data
    restart: unless-stopped

volumes:
  app-data:
