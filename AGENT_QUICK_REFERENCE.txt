╔══════════════════════════════════════════════════════════════════════════════╗
║                    MONEY MNGR PWA — AGENT QUICK REFERENCE                    ║
║                         (Print & Keep Visible)                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🚨 BEFORE YOU START (MANDATORY)                                              │
└──────────────────────────────────────────────────────────────────────────────┘
  1. git pull origin main
  2. Read CLAUDE.md → ACTIVE SESSION LOG
  3. Read CLAUDE.md → HANDOFF NOTES (latest)
  4. Run: npm run dev (confirm it works)
  5. Ask human if ANYTHING is unclear

┌──────────────────────────────────────────────────────────────────────────────┐
│ ⚡ GOLDEN RULES (NEVER VIOLATE)                                              │
└──────────────────────────────────────────────────────────────────────────────┘
  ✅ Follow CLAUDE.md exactly — no improvisation
  ✅ Fix P0 before P1, P1 before P2
  ✅ Use useLiveQuery for all DB reads (not useState)
  ✅ Never break double-entry ledger (balance = critical)
  ✅ Increment cacheId in next.config.js after UI changes
  ✅ Test with sample.csv (13,350 rows) after CSV changes
  
  ❌ Never install packages without asking human
  ❌ Never modify DB schema without asking human
  ❌ Never skip smoke test before committing
  ❌ Never commit with TypeScript errors
  ❌ Never make architectural decisions alone

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 PRIORITY ORDER (Always Check CLAUDE.md First)                            │
└──────────────────────────────────────────────────────────────────────────────┘
  P0 (Critical) → Must fix immediately, blocks other work
  P1 (High)     → Important features, fix after P0
  P2 (Medium)   → Nice-to-have improvements
  P3 (Low)      → Tech debt cleanup

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🧪 5-MINUTE SMOKE TEST (Before Every Commit)                                │
└──────────────────────────────────────────────────────────────────────────────┘
  ☐ npx tsc --noEmit             → 0 errors
  ☐ npm run dev                  → Server starts
  ☐ Open localhost:3000          → Redirects to /transactions
  ☐ Add 1 transaction            → Appears immediately
  ☐ Check balance updates        → Real-time change
  ☐ Visit /accounts              → Page loads
  ☐ Visit /stats                 → Charts render
  ☐ Browser console (F12)        → 0 errors

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔧 COMMON COMMANDS                                                           │
└──────────────────────────────────────────────────────────────────────────────┘
  npm run dev                    # Start dev server (port 3000)
  npx tsc --noEmit               # TypeScript check
  npm run build                  # Production build test
  git log -5 --oneline           # Recent commits
  git status                     # Uncommitted changes
  git diff                       # Review changes
  ./generate-agent-context.sh    # Generate handoff report

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📝 COMMIT MESSAGE FORMAT                                                     │
└──────────────────────────────────────────────────────────────────────────────┘
  fix(component): brief description       # Bug fixes
  feat(page): brief description           # New features
  refactor(hook): brief description       # Code improvements
  docs: brief description                 # Documentation only

  ✅ GOOD: fix(csvImport): await categoryId before transaction insert (P0.2)
  ❌ BAD:  updated files

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🛑 WHEN TO HALT & ASK HUMAN                                                  │
└──────────────────────────────────────────────────────────────────────────────┘
  • CLAUDE.md doesn't explain your scenario
  • Need to install new npm package
  • Need to change database schema
  • Modifying balance/ledger logic
  • Build fails after pulling Agent1's code
  • Merge conflict occurs
  • TypeScript errors you can't resolve
  • Unsure if architectural pattern is correct
  • Task description is ambiguous

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📂 KEY FILES & LOCATIONS                                                     │
└──────────────────────────────────────────────────────────────────────────────┘
  CLAUDE_gitTracking_userTVM_v1_01032026.md  # Single source of truth
  src/lib/db.ts                              # DB schema (Dexie)
  src/lib/csvImport.ts                       # CSV import logic
  src/app/transactions/page.tsx              # Transaction list
  src/components/AddTransactionModal.tsx     # Add/edit form
  src/components/stats/*.tsx                 # All charts
  next.config.js                             # PWA config (cacheId here!)
  .data/*.csv                                # Master database (NEVER delete)

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔍 KNOWN ISSUES (Check Before Reporting)                                    │
└──────────────────────────────────────────────────────────────────────────────┘
  • Category shows "Unknown" in Stats → Use csvCategory fallback
  • Service worker cache stale → Increment cacheId
  • Sub-category not selectable → Filtered out in code (known)
  • Net Worth chart missing lines → Only 1 Line component (P2 task)
  • PERSON account type → May not render everywhere (P3 task)

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📤 HANDOFF TEMPLATE (After Finishing Task)                                  │
└──────────────────────────────────────────────────────────────────────────────┘
  ## 🤖 AGENT[X] HANDOFF SUMMARY
  
  **Completed:**
  - ✅ [Task] — Modified [file] (lines X-Y)
    Changed: [what]
    Tested: [how]
  
  **Next Agent Should Do:**
  - [ ] [Next priority from CLAUDE.md]
    File: [path]
    Solution: [approach]
  
  **Build Status:**
  - ✅/❌ npm run dev
  - ✅/❌ npx tsc --noEmit
  
  **Commit:** [hash]

┌──────────────────────────────────────────────────────────────────────────────┐
│ 💡 DEBUGGING TIPS                                                            │
└──────────────────────────────────────────────────────────────────────────────┘
  • Balance wrong? → Check double-entry logic in useTransaction.ts
  • Chart not showing? → Check browser console for data errors
  • CSV import fails? → Test with sample.csv (known-good format)
  • Build slow? → Clear .next/ folder and rebuild
  • Stale content? → Increment cacheId, hard refresh (Ctrl+Shift+R)

┌──────────────────────────────────────────────────────────────────────────────┐
│ ⚖️ DECISION HIERARCHY (Who Decides What)                                    │
└──────────────────────────────────────────────────────────────────────────────┘
  1. CLAUDE.md          → Architectural patterns, coding standards
  2. Human (you)        → New features, DB schema, package additions
  3. Agent (Claude)     → Implementation details within existing patterns
  
  If CLAUDE.md and Human conflict → Human wins
  If Agent uncertain → Always ask Human

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 SUCCESS CRITERIA (Every Commit Must Meet)                                │
└──────────────────────────────────────────────────────────────────────────────┘
  ☐ 0 TypeScript errors (npx tsc --noEmit)
  ☐ 0 console errors in browser
  ☐ Smoke test passes (all 7 items)
  ☐ CLAUDE.md updated (session log + handoff notes)
  ☐ Commit message follows format
  ☐ cacheId incremented if UI changed
  ☐ git diff reviewed (no accidental files)

╔══════════════════════════════════════════════════════════════════════════════╗
║  🚀 AGENT WORKFLOW IN 3 STEPS                                                ║
║  1. READ  → CLAUDE.md (session log, handoff notes, priorities)              ║
║  2. CODE  → Follow existing patterns, ask if uncertain                       ║
║  3. TEST  → Smoke test, commit, update CLAUDE.md, push                       ║
╚══════════════════════════════════════════════════════════════════════════════╝

Version 1.0 | Last Updated: 2026-03-02 | Keep this visible while working!
